import { FilterFlightPipe } from './filter-flight.pipe';

describe('FilterFlightPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFlightPipe();
    expect(pipe).toBeTruthy();
  });
});
